*.iml
.gradle
# Third-party exercise dataset reference data (used only as offline inspiration when
# writing our own catalog entries — not redistributed, see scripts/ПРОМПТЫ_ДЛЯ_CHATGPT.md)
/scripts/exercisedb_raw.json
# ChatGPT web-automation session data — contains your logged-in browser profile, never commit
/scripts/.chatgpt_browser_profile/
/scripts/.chatgpt_gen_state.json
/scripts/__pycache__/
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
# Ignore all module build outputs
**/build/
**/.transforms/
**/intermediates/
**/outputs/
*.apk
*.dex
/captures
.externalNativeBuild
.cxx
local.properties

# Local environment overrides
.env.local
server/.env.local

vtempe-release.jks

# Firebase config — contains this project's client API key. Not a secret Google requires you to
# keep confidential (it's restricted by package name/SHA-1 in the Firebase console, not by
# secrecy), but this repo is public and there's no reason to publish it either — each dev
# downloads their own copy from https://console.firebase.google.com (Project settings > your
# Android app > google-services.json).
app-android/google-services.json

# iOS secrets — add API_BASE_URL and APP_TOKEN manually via Xcode Info tab
iosApp/Vtempe/Vtempe/Info.plist

# AI-generated source images (not used by app, only composeResources/ is used)
images/

# Project documents
AI_Fitness_KMM_TZ.pdf

# IDE settings (auto-generated by Android Studio)
.kotlin/
.idea/

# Claude Code worktrees and temp files
.claude/worktrees/
livs-hpmont/
V-Tempe/

# Logs and temp files
*.log
temp.txt
T3.txt

# LLM raw-response telemetry (LlmRawStore) — can contain user health data reflected back
# by the model (injuries, chat replies). Off by default (LLM_RAW_STORE_ENABLED), but never
# commit it even when enabled locally for debugging.
logs/
server/logs/
server/server/logs/
